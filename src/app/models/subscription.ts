export interface Subscription {
    gymid: string,
    employeeName: string,
    receipt: string,
    amountPaid: Number,
    startDate: Date,
    //validity: Date,
    status: string,
    comments: string
}